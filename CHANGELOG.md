# 更新日志

本文件记录版本级的功能清单。

## v0.1.0（2026-08-27）

测试版本，尽量在虚拟化环境中运行。以下为当前版本实现的完整功能清单。

### 核心

- **统一 Agent 循环**：原生 provider 工具调用（OpenAI / Anthropic / Gemini 协议适配，含原生 function calling 与流式 reasoning 透传）；循环无步数硬上限，由工具调用数 / 墙钟 / 累计 token 预算兜底
- **上下文管理**：轻量压缩（压力门控）+ 全量压缩 + 紧急截断；工具结果截断与 >64KB 大结果独立存储（前后 8KB 摘要）
- **重试与降级**：LLM 调用统一重试（指数退避、尊重 Retry-After）；流式已转发后不重试（防重复输出）；仅适配器未实现时降级非流式
- **运行中模型热切换**：switch_llm 挂起为 pending，run 边界原子应用；会话级模型/推理等级偏好持久化

### 会话与流式

- **统一会话模型**：Conversation / Turn / Node 以 SQLite（WAL）为唯一权威；主会话、工作区会话、渠道会话（飞书/微信）同一执行纪律
- **队列与插队**：消息统一入队（幂等键、饱和反馈）；运行中 Steering 两阶段注入（打断 → 工具自然结束 → 注入续跑）；队首倒计时自动分派
- **流式推送**：node.delta 合批（首 token 立即 + 100ms 窗口）、有界 SSE 订阅队列 + version_gap 快照自愈、Last-Event-ID 断线重放、Outbox 崩溃补发（死信终态可辨识）
- **图片消息**：WebUI 图片+文字经队列信封发送（≤4 张/≤4MB/白名单 mime），原图落盘、历史缩略图、回放占位降级；会话删除级联清理图片
- **停止与隔离**：协作式停止（进程组强杀即时生效）+ 停止/Steering 双看门（10s）+ 硬超时 zombie 隔离 + 迟到事件按 run 归属丢弃（防污染后续 Turn）
- **工具节点终态兜底**：轮次终态时未返回的工具卡自动收口为失败态，不再出现永久"执行中"

### 工具与安全

- **四档权限**（readonly / ask / allow / unreviewed）：唯一权威 PolicyEngine，能力分类裁决（fs/exec/net/proc），WebUI 顶栏按会话即时切档；WebUI 审批桥（fail-closed 300s，跨会话归属校验）
- **L2 沙箱**（可选）：命令/代码/进程输入/写盘/外发内容硬检查；规则热更（mtime 失效）
- **出站安全**：safe_http SSRF 防护（DNS 解析逐跳校验、rebinding 对端复核、响应体上限）；内网段按配置放行（本地工具默认开），云元数据端点恒拦
- **视觉白名单**：文件源图片的四档权限感知（unreviewed 之外按边界放行）
- **Hook 体系**：8 类事件 hook + 内置敏感词/危险命令过滤器，`/hook reload` 热重载
- **密钥安全**：入库脱敏（记忆与工具输出同一套凭据正则）、配置 API 掩码回写、日志 0600 打码

### 工作区

- **项目工作区**：Workspace / Profile / Session / Snapshot 四层模型；快照冻结执行上下文（模型/权限/MCP/工具集），配置改动自动重建 Agent
- **多智能体编辑**：Agent Profile 管理，system_prompt 热刷新（Plan/Goal 与普通轮次使用最新模板）
- **项目文件浏览**：路径校验（防穿越/软链逃逸）

### 任务自动化

- **Plan**：预览 → 批准 → 分步执行（权限感知的工具子集、孤儿任务看门、活动环形缓冲实时进度）
- **Goal**：目标多轮自主续跑（轮次预留/上限、完成判定、暂停审计、续跑快照）
- **Subagent**：子任务委派（父会话权限继承、审批归属传播、终态归档对账）
- **定时任务**：cron 调度（isolated 会话、投递重试、看门狗）；**心跳**：HEARTBEAT.md 清单驱动的周期自检（忙卡死自愈）

### 记忆

- **跨会话记忆**：BM25 + jieba 中文分词检索（memory_search），权重反馈（memory_update）；主会话共享池 + 工作区隔离池
- **内容形态**：用户提问 / 终答 / 工具短桩（报错放宽）；过程旁白与系统注入不入库
- **保留期**：≥15 天负分、≥30 天零分自动清除；正权重永久；每日自动清理

### 运维

- **保留期体系**：终态任务/Artifacts 30 天、system 会话 7 天、Outbox/幂等/回执 24h、任务域 sessions 表 30 天（无引用）——每小时循环
- **磁盘自洁**：数据库空闲页每日自动 VACUUM 收缩（空闲>30% 且 >50MB 触发）；迁移备份轮换（最新 1 份、30 天上限）
- **WebUI 安全**：非环回访问 token/IP 白名单（AND）、调试通道默认关、健康与指标端点可选 Bearer、Origin/Content-Type 校验
- **命令面**：`run / login weixin / doctor / migrate-sessions`；`python agent.py init` 交互式向导（稀疏写入，默认值随版本演进）
- **可观测**：/metrics 指标（turn 时延/首 delta/工具统计）、turn 级 trace、统一 run/turn 日志上下文

### Web UI

- React 18 + TypeScript + Vite；虚拟列表消息流（展开/折叠零错位）、块级增量 Markdown 渲染
- 统一 Gateway Store：rAF 合帧、(node_id, seq) 增量去重、两级版本门控 + 快照自愈；恢复流程按会话归属校验 + 版本水位只进不退（多会话同时运行切换页面不串扰）
- 队列面板（倒计时、"插入 = 立即发送"统一语义：运行中 Steering 注入 / 空闲立即分派、停止看门后 Steering 状态收口防队列卡死）、Steering 提示、Turn 状态条、上下文占用弹层、目标条、审批/提问卡片
- 会话/工作区/智能体/Prompt/Skills/设置/定时任务/状态 9 条路由（Plan/Goal 管理页已移除，功能内聚在会话页浮层/GoalBar/投影卡）

### 质量

- 后端 pytest 776 用例（含统一会话/权限/清理/图片/并发回归），ruff 全范围零告警
- 前端 vitest 290 用例 + tsc 零错误（含多会话切换竞态回归）；Playwright 真实浏览器 e2e（种子网关直连真实 API：主会话/工作区/图片/路由/生命周期）
- CI：push/PR 自动运行后端与前端检查（Python 3.14 对齐本地）
