# Full feature installation for the Gateway and Agent runtime.
# The canonical dependency metadata lives in pyproject.toml; this compatibility
# file intentionally delegates to the optional `all` extra.
-e .[all]
